import { defineComponent } from "vue";
export default defineComponent({
    name: "Atricle",
    setup() {
        return () => (
            <div>
                <h1>Atricle</h1>
            </div>
        );
    }
})