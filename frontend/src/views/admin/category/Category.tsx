import { defineComponent } from "vue";
export default defineComponent({
    name: "Category",
    setup() {
        return () => (
            <div>
                <h1>Category</h1>
            </div>
        );
    }
})